# Stamped Results

This file contains **stamped canonical results only**. Current validated/provisional evidence is summarized in `README.md` until the corresponding code, checkpoint, protocol and commit are frozen.

## Current status

No global baseline suite is stamped yet.

`HSTU_CANONICAL_v1` is closest: quality reproduction and serving parity are complete, but durable mature checkpoints must be regenerated before the final stamp.

## Stamp requirements

Each stamped row records model/version, dataset/source, preprocessing/split, evaluation protocol, seeds, parameter count, training objective, checkpoint id, commit SHA, hardware/runtime, quality metrics, training cost, request-from-history latency, cached streaming latency, state/cache memory, and comparability notes.
