# Paper workspace

Keep manuscript claims synchronized with stamped evidence.

Working framing:

> Sparse Walker is a dynamic-support recurrent/state-space architecture: it learns not only latent-state values, but which latent state variables are active at each timestep.

Potential contribution ladder:

1. recommender quality + bounded-state serving Pareto;
2. formal dynamic-support SSM + parallel sparse scan;
3. local/backward-pass-free learning.

Do not promote exploratory claims into the manuscript until the corresponding roadmap item has empirical support.
